# Industrial drying material library agent - Milestone 1
# Runtime target: CPU only, 16GB RAM, no NVIDIA GPU, no CUDA required.

# --- core ---
pydantic>=2.7
PyYAML>=6.0
python-dotenv>=1.0
httpx>=0.27

# --- ui ---
gradio>=4.44

# --- douyin discovery (Milestone 3.5) ---
# Browser based discovery of public Douyin search results. After installing,
# fetch the browser once:  python -m playwright install chromium
playwright>=1.45

# --- media ---
# FFmpeg / ffprobe binaries must be installed on the OS (they are not pip packages).
scenedetect>=0.6.4
opencv-python-headless>=4.9
imagehash>=4.3
Pillow>=10.0

# --- measured subtitle analysis (Milestone 6) ---
# PP-OCR detection + recognition on CPU through ONNX runtime (no GPU needed).
# The pipeline degrades gracefully to OpenCV region proposals - or to the VLM
# verdict - when this package is missing.
rapidocr-onnxruntime>=1.3

# --- optional cloud subtitle erase (Milestone 9.8) ---
# Official Volcano Engine SDK, used only when --engine volcengine is selected.
# The local subtitle_cleanup_v1 works without this package.
volcengine>=1.0.228

# --- tests ---
pytest>=8.0
pytest-asyncio>=0.23
